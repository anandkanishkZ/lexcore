node_modules/
dist/
.env
*.js.map
uploads/

# Tesseract.js auto-downloads its ~5MB OCR language model on first use
# (see utils/text-extraction.util.ts) — a runtime cache, not source.
.cache/
*.traineddata
backups/

# Firebase Admin SDK service account key (push notifications) — grants full
# send-as-this-project push access; never commit it, see .env.example.
firebase-service-account.json

# Jest coverage output
coverage/
